#include <iostream>
#include <cmath>
#include <vector>
using namespace std;
int main() {
  int n, m, t;
  cin>>n >>m >>t;
  vector<vector<int> > rc(n, vector<int> (2));
  for(int i=0; i<n; ++i)
    cin>> rc[i][0]>> rc[i][1];
  
}